# For local `streamlit run` and Streamlit Community Cloud.
# Streamlit-in-Snowflake uses environment.yml instead (Anaconda channel).
streamlit>=1.40.0
snowflake-connector-python>=3.7.0
snowflake-snowpark-python>=1.18.0
pandas>=2.0.0
reportlab>=4.0.0
